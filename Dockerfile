# Указываем базовый образ
FROM nginx:alpine

# Копируем файлы
COPY . .

# Указываем порт, который будет открыт для внешнего мира
EXPOSE 80

# Команда для запуска Nginx
CMD ["nginx", "-g", "daemon off;"]